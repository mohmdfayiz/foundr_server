import { RequestHandler } from "express";
export declare const getArticles: RequestHandler;
export declare const publishArticle: RequestHandler;
export declare const updateVisibility: RequestHandler;
